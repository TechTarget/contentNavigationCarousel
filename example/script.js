$(document).on('ready', function () {
	
	$('.contentNavigationCarousel').contentNavigationCarousel({
		autoplay: true,
		autoplaySpeed: 10000,
		mouseEvent: 'hover',
		switchSpeed: 500,
		equalizeHeights: true
	});

});